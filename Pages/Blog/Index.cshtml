@page
@model asp14_Validation.Pages_Blog.IndexModel

@{
    ViewData["Title"] = "Index";
    int i = 1;
}
@* 
@{
    var pagingmodel = new asp14_Validation.Models.PagingModel()
    {
        currentpage = 3,
        countpages = 20,
        generateUrl = (int? p) => "trang " + p
    };
}

<partial name="_PagingXTL" /> *@

<h1>Danh sách bài viết</h1>
<form method="get" asp-page="./Index">
    <label>Tìm kiếm</label>
    <input name="SearchString" value="" />
    <button type="submit">Tìm</button>
</form>

<p>
    <a asp-page="Create">Tạo bài viết mới</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>STT</th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].Created)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Article) {
        <tr>
            <td>@(i++)</td>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Created)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Chỉnh sửa</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Xem</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Xóa</a>
            </td>
        </tr>
}
    </tbody>
</table>
