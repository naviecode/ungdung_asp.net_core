@using Microsoft.AspNetCore.Identity
@using asp14_Validation.Models

@* Inject 2 dịch vụ quản lý user *@
@inject SignInManager<AppUser> signInManager
@inject UserManager<AppUser> userManager


@{
    //Kiểm tra user đã đăng nhập hay chưa true/false
    //signInManager.IsSignedIn(User);
    //Lấy ra thông tin của user
    //userManager.GetUserName(User);
}

<ul class="navbar-nav">
    @if(signInManager.IsSignedIn(User))
    {
        <li class="nav-item">   
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index">
                Xin chào - @userManager.GetUserName(User);
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Logout">Dang xuat</a>
        </li>
    }else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Dang ky</a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Dang nhap</a>
        </li>
    }
    
</ul>